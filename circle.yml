machine:
  timezone: Asia/Tokyo

dependencies:
  override:
    - sudo pip install awscli
  post:
    - aws configure set region ap-northeast-1

deployment:
  test:
    branch: master
    commands:
      - uname -a
  production:
    branch: release/prd
      - uname -a
  staging:
    branch: release/stg
      - uname -a
  development:
    branch: release/dev
      - uname -a
